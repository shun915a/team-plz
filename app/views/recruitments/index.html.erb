<%= render 'shared/header' %>

<section class="video-wrap">

  <%= video_tag("video.mp4", poster: "video.jpg", autoplay: true, loop: true, muted: true, class: "bg-video") %>

  <div class="video-contents">
    <div class="copy">
      <h1>FIND YOUR</h1>
      <h1>BUDDY</h1>
    </div>
      <% unless user_signed_in? %>
        <div class="btn-border">
          <%= link_to 'SIGN UP', new_user_registration_path, class: "video-sign-up-btn" %>
        </div>
      <% end %>
  </div>
</section>


<section class="container" id="root"> 
  <h1 class="list-title-text">// LATEST RECRUITMENTS</h1>
  
  <h1 class="title title-top">FRIEND</h1>

  <%= render 'friends/list' %>

  <h1 class="title">PARTY</h1>

  <%= render 'parties/list' %>

  <h1 class="title">TEAM</h1>

  <%= render 'teams/list' %>

  <h1 class="title">SCOUT</h1>

  <%= render 'scouts/list' %>


  <ul class="item-lists">
    <%# render partial: 'friends/friend', collection: @friends%>
    <%# render partial: 'parties/party', collection: @parties %>
    <%# render partial: 'teams/team', collection: @teams %>
    <%# render partial: 'scouts/scout', collection: @scouts %>
  </ul>
</section>

